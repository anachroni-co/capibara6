# Configuración SMTP para envío de emails
# Copia este archivo a .env y configura tus credenciales

# Servidor SMTP de capibaragpt.com (con soporte SSL)
SMTP_SERVER=smtp.dondominio.com
SMTP_PORT=587

# Credenciales del email
SMTP_USER=es2030@capibaragpt.com
SMTP_PASSWORD=Es2030$$

# Email desde el que se envían los mensajes
FROM_EMAIL=es2030@capibaragpt.com

# Configuración de modelo remoto (Ollama)
OLLAMA_BASE_URL=http://10.164.0.9:11434
OLLAMA_MODEL=gpt-oss:20b
# Opcional: OLLAMA_TEMPERATURE=0.8
# Opcional: OLLAMA_TOP_P=0.9

# Configuración del router de modelos
MODEL_CONFIG_PATH=backend/model_config.json
DEFAULT_MODEL_TIER=fast_response
STREAMING_ENABLED=true
FALLBACK_ENABLED=true

# NOTA: Servidores SMTP disponibles para capibaragpt.com
# Con soporte SSL (recomendado):
#   - smtp.dondominio.com (puerto 587) ✅ CONFIGURADO
#   - smtp.panel247.com (puerto 587)
# Sin soporte SSL:
#   - smtp.capibaragpt.com (puerto 25 o 587)